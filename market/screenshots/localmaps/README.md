# LocalMaps screenshots

Placeholder directory. Add the following WebP assets before the first
market release:

- `featured.webp` — hero image referenced as `featuredImage` in
  `market/catalog.json` and `featuredImage` in the chart's
  `OlaresManifest.yaml`.
- `1.webp`, `2.webp`, `3.webp` — promotional screenshots referenced in
  `promoteImage`.

Recommended size: 1280x800 (16:10), WebP q=85. Take them from the UI
running at `http://localhost:8080` after installing a region.

NEEDED (primary): capture final screenshots once Phase 1-5 UIs settle
(map canvas, search panel, directions panel, admin region install
progress, settings).
