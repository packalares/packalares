apiVersion: v1
kind: ConfigMap
metadata:
  name: vllm-model-config
  namespace: {{ .Release.Namespace }}
data:
  MODEL_NAME: {{ .Values.model.name | quote }}
  HF_REPO: {{ .Values.model.hfRepo | quote }}
