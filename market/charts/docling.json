{
  "categories": [
    "Utilities"
  ],
  "cfgType": "app",
  "chartName": "docling",
  "dependencies": [
    {
      "name": "packalares",
      "type": "system",
      "version": ">= 1.0.0"
    }
  ],
  "description": "Local document conversion service: PDF/Office/images/CSV -> markdown, HTML, text, structured JSON, layout-JSON, or CSV/tables. OCR on your GPU-queued Studio/Ollama (external) or an in-pod Ollama (local).",
  "developer": "docling-project",
  "doc": "https://docling-project.github.io/docling/",
  "entrances": [],
  "featuredImage": "https://avatars.githubusercontent.com/u/195456102?s=200&v=4",
  "fullDescription": "## OVERVIEW ##\nDocling is a document CONVERSION service. It turns PDFs, Office files, images and CSV into markdown, HTML, plain text, structured JSON, layout JSON, or CSV/HTML tables. Text documents parse on the CPU; OCR/vision recognition runs on a pluggable LLM backend (your GPU-queued ComfyUI Studio/Ollama, or an in-pod Ollama). Nothing leaves the machine.\n\n## OUTPUT FORMATS (POST /v1/convert, field 'format') ##\n- markdown / text - full-document transcription\n- html - POSITIONED layout: each element placed at its bbox, mirrors the page\n- json - structured field extraction; pass an optional JSON 'schema' to shape it\n- csv - tables extracted (text PDFs use deterministic geometry, no LLM; scanned docs use the recognizer)\n\n## RECOGNIZERS (POST /v1/convert, field 'recognizer') ##\n- dots (default) - end-to-end: the whole page becomes the output in one call. Best all-round.\n- paddle-vl - element pipeline: a CPU layout model detects regions, each region is OCR'd, then reassembled (structured OTSL tables). Requires layout.enabled.\n\n## ENDPOINTS ##\n- POST /v1/convert  {sources:[{filename, base64_string}], format, recognizer?, schema?, options?} -> {document:{content}, ...}\n- POST /v1/layout   {filename, base64_string} -> {boxes:[{bbox, category, score}]} (CPU layout)\n- POST /render      returns page images (base64 PNG) for scanned docs\n- GET  /health      status + active backend + supported formats\n\n## CONFIGURATION (config.json) ##\nAll behavior - backend, recognizers, extractor, CPU layout model, prompts, formats, thresholds - lives in config.json, mounted at /wrapper/config.json (the docling-wrapper ConfigMap). Edit that ConfigMap and 'kubectl rollout restart deploy/docling' to change prompts or models. NO secrets are stored there.\n\n## LLM BACKEND: EXTERNAL vs LOCAL (chart value backend.mode) ##\nEXTERNAL (default): recognition runs on your ComfyUI Studio LLM API (auto: http://comfyuistudio.<namespace>:3002/api/llm/v1), which owns the single-GPU queue. The docling pod stays CPU-only. Needs the API-key Secret (below). The model ids named in config.json (hf.co/...) must already exist on that Ollama.\n\nLOCAL (standalone / independent install): an in-pod Ollama sidecar serves the models on localhost:11434 (NOT exposed outside the pod). The pod then requires a GPU (runtimeClassName nvidia is added automatically). Models are SEEDED on first start: HuggingFace's own client downloads each GGUF (model + mmproj) and 'ollama create' registers it - because 'ollama pull hf.co/...' fails on HF's cross-host xet CDN ('blocked redirect to a different host'). Configure models in backend.localModels as {name, repo}; 'name' must match the recognizer/extractor 'local_model' in config.json, and 'repo' is an HF GGUF repo containing a model *.gguf plus an *mmproj*.gguf. The default seeds dots-ocr (the default recognizer); add olmocr-2 and paddleocr-vl repos to enable format=json and recognizer=paddle-vl in local mode.\n\nTO SWITCH: set backend.mode=local (and provide backend.localModels) at install/upgrade. Everything else - the wrapper, endpoints, config.json - is identical; only where the LLM runs changes.\n\n## CPU LAYOUT (chart value layout.enabled, default true) ##\nWhen enabled, an init container installs paddlepaddle+paddleocr (CPU, from PyPI) into the cache for the PP-DocLayoutV3 layout model. It is used ONLY by recognizer=paddle-vl and the csv/tables/layout formats. The default recognizer (dots) needs none of it - set layout.enabled=false for the leanest, fully CPU pod.\n\n## API KEY (external mode) ##\nThe API key is read at runtime from a Kubernetes Secret (default name 'docling-vlm', key 'STUDIO_API_KEY') and is never stored in the package. The CPU/text paths work without it.\n1. In ComfyUI Studio -> Settings -> API keys, create a key with the 'chat:write' scope.\n2. kubectl -n <namespace> create secret generic docling-vlm --from-literal=STUDIO_API_KEY='sk_...'\n3. kubectl -n <namespace> rollout restart deploy/docling\n",
  "hasChart": true,
  "icon": "https://avatars.githubusercontent.com/u/195456102?s=200&v=4",
  "license": [
    {
      "name": "MIT",
      "url": "https://github.com/docling-project/docling-serve#MIT-1-ov-file"
    }
  ],
  "limitedCPU": "4",
  "limitedMemory": "8Gi",
  "locale": [
    "en-US"
  ],
  "name": "docling",
  "permission": {
    "appCache": true,
    "appData": true,
    "userData": null
  },
  "promoteImage": [
    "https://avatars.githubusercontent.com/u/195456102?s=200&v=4"
  ],
  "rating": 0,
  "requiredCpu": "100m",
  "requiredDisk": "1Gi",
  "requiredMemory": "2Gi",
  "source": "olares",
  "sourceCode": "https://github.com/docling-project/docling-serve",
  "status": "active",
  "supportArch": [
    "amd64"
  ],
  "title": "Docling",
  "type": "app",
  "upgradeDescription": "1.2.20 - /health now reports whether the recognizer/extractor models (dots.ocr, PaddleOCR-VL, olmOCR-2) are present on the LLM backend (cached). Studio's docling connection-test surfaces missing/unreachable models instead of a bare OK.\n1.2.19 - Large-document handling: text-PDF converts no longer hang. Studio raises the convert timeout to 900s (a ~260-page statement converts on CPU in ~350s), and the wrapper fast-fails with a clear 413 for docs over max_doc_pages (config default 600) instead of timing out.\n1.2.18 - csv: 3 blank rows between tables and before the non-table text section (shared build_csv helper across text-PDF/Office/scanned paths).\n1.2.17 - Scanned csv now merges same-header tables into one (header kept once), like the text-PDF path / original docling_test.py; different-header tables (e.g. a product table vs a footer) stay separate.\n1.2.16 - Scanned csv now also appends the non-table text section (page headers, IBANs, etc.) - dots: text outside its <table> output; paddle-vl: non-table region OCR. Matches the text-PDF/Office csv behavior.\n1.2.15 - csv: (a) Office files (docx/xlsx/pptx) now work via docling's table export (was a crash on the PDF-geometry path); PDF csv unchanged. (b) Append the unique non-table text section (titles, IBANs, etc.) after the tables, restoring the original docling_test.py behavior.\n1.2.14 - Remove the unused layout-json and tables formats (nothing consumed them). Formats now: markdown, html, text, json, csv. markdown/text/csv/json/html unchanged.\n1.2.13 - Drop column-index rows (e.g. '1 2 3 4 5' under a header) from scanned csv/tables output; header kept.\n1.2.12 - Proper CSV: every row padded to a uniform column count (rectangular) and a plain \\n line terminator instead of CRLF.\n1.2.11 - Scanned csv/tables now respect the recognizer: dots does ONE end-to-end call per page and its HTML tables are parsed to rows (was 26 wasted calls + empty output); paddle-vl keeps the per-region OTSL pipeline. Text-PDF csv unchanged (CPU geometry).\n1.2.10 - Positioned-HTML fixes. Scanned pages: html now uses the element pipeline (CPU layout -> per-region OCR/table -> placed at bbox) instead of asking the recognizer for layout-json (which paddle-vl can't produce -> empty). Text PDFs: tables render their real grid, font is sized per text line (no oversized type), and text boxes grow so nothing is clipped. Shared recognize_page_regions core for element markdown/csv and html.\n1.2.9 - format=html now produces POSITIONED HTML: every element placed at its bbox on a per-page canvas, mirroring the original page layout (text PDFs via docling on CPU; scanned pages via the recognizer's layout mode). Replaces the previous semantic flow HTML.\n1.2.8 - Only report _recognizer when a recognizer model actually ran. CPU pipelines (cpu/cpu-geometry/cpu-layout) and the json extractor no longer echo a misleading 'dots'.\n1.2.7 - Re-add the _ms elapsed-time field to /v1/convert and /v1/layout responses (server-measured conversion time).\n1.2.6 - Wrapper refactor + fixes. (1) csv/tables no longer convert the PDF twice -> ~2x faster (8s->4s on a 3-page statement). (2) json and layout-json now read ALL pages, not just page 1. (3) text PDFs get layout-json from docling on CPU (fast, always-valid) instead of a slow, flaky GPU call; json on a text PDF feeds text to the extractor. (4) blocking work runs in a threadpool so long conversions no longer stall other requests. Shared helpers, dead code removed.\n1.2.5 - Collapse to a single conversion endpoint. Removed the back-compat POST /v1/convert/source; all document ingestion (chat + LLM-API attachments) now goes through POST /v1/convert (format=markdown). No behavior change for callers.\n1.2.4 - Fix: pin huggingface-hub <1.0 in the CPU-layout init. paddleocr/paddlex declare it unpinned and were pulling 1.x into the shared PYTHONPATH cache, shadowing the image's 0.34.x and breaking transformers -> every /v1/convert returned a 502 on import ('huggingface-hub>=0.34,<1.0 is required'). Only affected installs with layout.enabled=true.\n1.2.3 - Document conversion service: pluggable LLM backend (external Studio/Ollama or in-pod local Ollama), selectable recognizer (dots end-to-end / paddle-vl element pipeline), and multi-format /v1/convert (markdown, html, text, json, layout-json, csv, tables). All behavior in config.json. Local mode seeds Ollama via HuggingFace download + 'ollama create' (works around the hf.co xet redirect). CPU layout (PP-DocLayoutV3) gated by layout.enabled. Text-PDF tables -> CSV via deterministic geometry. Back-compat /v1/convert/source retained.\n1.1.3 — Add a /render endpoint (page images) enabling Studio's fast path: a scanned PDF sent to a vision model is answered from page images in one call, skipping OCR. 1.1.2 — Scanned pages are transcribed by calling the vision model directly (render + OpenAI request) instead of docling's VLM pipeline, avoiding a docling markdown-reassembly crash; output code-fence stripping. 1.1.1 — Auto-download CPU-pipeline models via an init container. 1.1.0 — Scanned PDFs/images parse on-GPU via an Ollama vision model (through the Studio LLM API queue); text documents use the fast CPU pipeline. Add a Studio API key as the 'docling-vlm' Secret to enable the scanned-doc path.\n",
  "version": "1.2.20",
  "versionName": "1.2.20",
  "website": "https://docling-project.github.io/docling/"
}